-- Omarchy defaults CapsLock to Compose and moves Caps Lock to both Shift keys.
-- Keep the conventional keyboard behavior in this NixOS profile instead.
hl.config({
  input = {
    kb_options = "",
  },
})

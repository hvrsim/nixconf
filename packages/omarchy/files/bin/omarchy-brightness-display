#!/usr/bin/env bash

set -euo pipefail

case "${1:-}" in
  off) exec hyprctl dispatch dpms off ;;
  on) exec hyprctl dispatch dpms on ;;
  *)
    echo "Usage: omarchy-brightness-display <off|on>" >&2
    exit 2
    ;;
esac
